import React from 'react';

function Completed() {
  // const toDo = (markDone)
  return (
    <div >
      {/* <span title="Completed"
        onClick={(e) => markDone(task.id)}
      ></span> */}
    </div>
  );
}
export default Completed;